- if user_signed_in?
  - if @recipe.bookmark_by?(current_user)
    = link_to icon('fas', 'star', class: 'no'), recipe_bookmarks_path(@recipe.id), method: :delete, remote: true
  - else
    = link_to icon('fas', 'star', class: 'yes'), recipe_bookmarks_path(@recipe.id), method: :post, remote: true
